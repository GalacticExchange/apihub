Connecting...

:javascript
  var url = "#{@url}";
  var token = "#{@user_token}";

  $( document ).ready(function() {
    document.cookie = "token="+token+"; Path=/; Domain=#{@webproxy_host}";
    //location.href = url;
  });
