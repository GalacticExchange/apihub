- if item.status == Team::STATUS_NOT_VERIFIED
  .label.label-success not verified

- if item.status == Team::STATUS_DISABLED
  .label.label-danger disabled

- elsif item.status == Team::STATUS_DELETED
  .label.label-default deleted

- elsif item.status == Team::STATUS_PENDING_CREATE
  .label.label-danger pending create

- elsif item.status == Team::STATUS_PENDING_DELETE
  .label.label-danger pending delete

- elsif item.status == Team::STATUS_ACTIVE
  .label.label-primary active