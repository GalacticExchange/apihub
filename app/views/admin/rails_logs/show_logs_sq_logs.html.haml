.page-header
  .row
    .col-md-7
      %h1
        Sidekiq loglog
      %p
      %b file name:
      = File.basename("#{Rails.root}/log/apihub-sidekiq-#{Rails.env}-all_nolog-0.log")

%textarea{:style => "width: 100%;", rows: 30}
  = @text