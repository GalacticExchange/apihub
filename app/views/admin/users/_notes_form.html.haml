
= simple_form_for([:admin, @user], url: admin_update_user_notes_path, method: :put) do |f|
  = render 'shared/form_errors', {f: f}

  = f.input :admin_notes, as: :text
  .button_place.padd_top_md.padd_bott_big
    = f.button :submit, :value=>"Submit"


