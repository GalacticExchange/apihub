-#= link_to profileshow_path, :class => (s = bt == "show" ? "list-group-item active" : "list-group-item") do
  Profile
.main{:style=>"padding-top: 4px"}
  .block1{:style=>"display:inline; margin-left:5px"}
    = link_to user_path(:name => @user.username), :style=>"text-decoration: none;" do
      = image_tag avatar_url(@user, :thumb), :width=>30, :style=>"margin-bottom: 12px", :class=>"img-circle"
      %h2{:style=>"display:inline; opacity: 0.6"} Profile
      -#=image_tag "back_rev.png", :class=>"back_pic", :width=>10,:style=>"margin-bottom: 10px; margin-left: 4px; opacity: 0.6"

  = link_to profileedit_path, :class => (s = bt == "edit" ? "list-group-item active" : "list-group-item") do
    Main Info
  = link_to avataredit_path, :class => (s = bt == "editavatar" ? "list-group-item active" : "list-group-item") do
    Avatar
  = link_to profileeditpassword_path, :class => (s = bt == "editpwd" ? "list-group-item active" : "list-group-item") do
    Password



