:javascript
  GexUtils.currentTeam = #{@current_user.team.to_hash.to_json rescue nil};
  GexUtils.currentUser = #{@current_user.to_hash.to_json rescue nil};

.flex-container(ng-app="clustergxSettings")
  .inline-flex(data-block="main_menu")
    .col-xs-12.no_padd.install_left_col.main_div_size.flex_col_sm
      %profile-menu.row{:style=>"width: 100%; overflow-x: hidden;"}

  .inline-flex{:style=>"flex-grow: 1;"}
    .col-xs-12.main.main_div_size(data-block="main_content")
      %ng-view.fade-pg
        Loading...